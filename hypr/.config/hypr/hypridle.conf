general {
    lock_cmd = pidof hyprlock || hyprlock       # Avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session    # Lock before suspend.
    after_sleep_cmd = hyprctl dispatch dpms on  # To avoid having to press a key twice to turn on the display.
}  

# check /sys/class/leds to find the name of your kbd_backlight
listener {
    timeout = 5                                                   # 5 seconds
    on-timeout = brightnessctl -sd asus::kbd_backlight set 0      # Turn keyboard backlight off
    on-resume = brightnessctl -rd asus::kbd_backlight             # Restore to previous (33%)
}

listener {
    timeout = 300                                # 5 min.
    on-timeout = brightnessctl -s set 10%        # Set monitor backlight to minimum, avoid 0 on OLED monitor.
    on-resume = brightnessctl -r                 # Monitor backlight restore.
}

listener {
    timeout = 900                                                     # 15 min
    on-timeout = hyprctl dispatch dpms off                            # Screen off when timeout has passed
    on-timeout = loginctl lock-session
    on-resume = hyprctl dispatch dpms on && brightnessctl -r          # Screen on when activity is detected after timeout has fired.
}

# listener {
#     timeout = 1800                                # 30 min
#     on-timeout = systemctl suspend                # Suspend PC
# }
